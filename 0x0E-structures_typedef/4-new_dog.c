#include "dog.h"

/**
* new_dog - fn name
* @name: ogdv
* @age: fgs
* @owner: sdf
* Return: void
*/

dog_t *new_dog(char *name, float age, char *owner)
{
	if (!name || !owner || age < 0)
		return (NULL);
	
}

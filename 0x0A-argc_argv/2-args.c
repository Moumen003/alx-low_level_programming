#include <stdio.h>

/**
* main - fn name
* @argc: hanshoof
* @argv: elfeha
* Return: teslm
*/

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		printf("%s\n", argv[i]);
	}
	return (0);
}

#include "main.h"

/**
 * _islower - checks if lower case
 *
 * @c: checks input
 *
 * Return: return 1 if success
*/

int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
else
return (0);
}
}
